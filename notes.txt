// exports.selectArticleById = (article_id, article_name) => {
//     .modify(currentQuery => {
//       if (article_name) {
//         currentQuery.where("articles.article_id", article_id);
//       }
//     })
//     .then(([article]) => {
//       return article;
//     });
// };

/* MULTI STEP MODELS
​
need to validate something exists? maybe a user? maybe an article
​
// check exists (can you reuse previous model functionality)
// if exists -> continue on with your main query
// if doesn't exist -- reject -> or hopefully your reused model functionality will reject for you
​
// if you are successful in your main query return the results of that back
​
​
​
// you can then refactor the above using a promise all the syntax for which is below
*/
​
​
// Promise.all([firstValidator, secondValidator, etc...])
// .then([resultOfFirstValidator, resultOfSecondValidator, etc...] => {	
// 	// continue onto main query
// })